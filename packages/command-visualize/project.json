{
  "name": "@cplace-cli/command-visualize",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/command-visualize/src",
  "projectType": "library",
  "targets": {
    "build": {
      "executor": "nx:run-script",
      "outputs": ["{workspaceRoot}/dist/packages/command-visualize"],
      "options": {
        "script": "build"
      },
      "dependsOn": ["^build"]
    },
    "clean": {
      "executor": "nx:run-script",
      "options": {
        "script": "clean"
      }
    }
  }
}